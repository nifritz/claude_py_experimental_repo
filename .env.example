# Rete Docker esistente
# Trovala con: docker network ls
# oppure: docker inspect n8n | grep -i network
DOCKER_NETWORK_NAME=nome_rete_esistente

# Claude Code
ANTHROPIC_API_KEY=sk-ant-...

# Webhook deploy automatico (segreto condiviso con GitHub)
DEPLOY_SECRET=stringa_casuale_lunga_almeno_32_caratteri
